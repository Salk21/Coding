#include <iostream>
#include <cstdlib>
#include <math.h>
using namespace std;
int funn(int a)
{
	int b;
	if (a > 0)
	{
		b = 1;
	}
	if (a < 0)
	{
		b = -1;
	}
	if (a == 0)
	{
		b = 0;
	}
	return b;
}

int main()
{
	setlocale(LC_CTYPE, "rus");
	int a, c;
	cout << "Введите число x" << endl;
	cin >> a;
	c = funn(a);
	cout <<"signx = "<< c << endl;

}
